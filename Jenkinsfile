pipeline {
    agent any
    stages { 
        stage('working with variable') {
            steps {
              script {
                  a = 10
                  println "my variable value is ${a}"
              }
            }
        }
    }
}
